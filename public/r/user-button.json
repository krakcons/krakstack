{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "user-button",
  "title": "User Button",
  "description": "A Better Auth user menu with a central-auth-backed account dialog, localized labels, and sign-out handling.",
  "dependencies": [
    "@better-auth/api-key",
    "@krak-stack/auth",
    "@tanstack/react-table",
    "better-auth",
    "effect",
    "lucide-react",
    "react",
    "react-qr-code"
  ],
  "registryDependencies": [
    "badge",
    "button",
    "@krak-stack/data-table",
    "dialog",
    "dropdown-menu",
    "form",
    "separator"
  ],
  "files": [
    {
      "path": "src/services/auth/client/index.ts",
      "content": "import { apiKeyClient } from \"@better-auth/api-key/client\";\nimport { createAuthClient } from \"better-auth/react\";\nimport {\n  genericOAuthClient,\n  organizationClient,\n  twoFactorClient,\n} from \"better-auth/client/plugins\";\n\nexport const centralAuthBaseUrl = import.meta.env.VITE_KRAKSTACK_AUTH_URL;\n\nexport const centralAuthUrl = (path: string) =>\n  new URL(path, centralAuthBaseUrl).toString();\n\nexport const centralLoginUrl = (\n  callbackURL: string,\n  locale: \"en\" | \"fr\" = \"en\",\n) => {\n  const url = new URL(`/${locale}/sign-in`, centralAuthBaseUrl);\n  url.searchParams.set(\"redirect\", callbackURL);\n  return url.toString();\n};\n\nexport const authClient = createAuthClient({\n  baseURL: centralAuthBaseUrl,\n  fetchOptions: {\n    credentials: \"include\",\n  },\n  plugins: [\n    genericOAuthClient(),\n    twoFactorClient({\n      twoFactorPage: centralAuthUrl(\"/2fa\"),\n    }),\n    organizationClient(),\n    apiKeyClient(),\n  ],\n});\n",
      "type": "registry:file",
      "target": "services/auth/client/index.ts"
    }
  ],
  "docs": "## Overview\n\n`user-button` provides a compact authenticated-user dropdown for apps that use Krakstack Auth as a central identity provider. It reads the visible app session and central account state from `authClient`, including profile updates, linked accounts, password settings, two-factor authentication, and user API keys.\n\n## Features\n\n- Central Krakstack Auth session display\n- Central Krakstack Auth account mutations through `authClient`\n- Profile, linked accounts, password, 2FA, and API-key dialogs\n- Reconnect state when the central auth session is missing\n- Localized menu and form labels\n- Configurable sign-out redirect\n- Configurable dropdown side\n\n## Usage\n\n```tsx\nimport { UserButton } from \"@krak-stack/auth\";\n\n<UserButton signOutRedirect=\"/\" side=\"bottom\" />\n```\n\nSet `VITE_KRAKSTACK_AUTH_URL` to your central auth origin, for example `http://localhost:3001`.",
  "type": "registry:block"
}
