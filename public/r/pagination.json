{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "pagination",
  "title": "Pagination",
  "description": "Reusable Effect schemas and types for paginated API request and response envelopes.",
  "dependencies": ["effect@beta"],
  "files": [
    {
      "path": "src/lib/query.ts",
      "content": "import { Schema } from \"effect\";\n\nexport const Query = Schema.Struct({\n  page: Schema.Int.check(Schema.isGreaterThanOrEqualTo(0)),\n  pageSize: Schema.Int.check(Schema.isBetween({ minimum: 1, maximum: 100 })),\n}).pipe(\n  Schema.annotate({\n    identifier: \"Query\",\n    title: \"Query\",\n    description: \"Zero-based page request parameters for list endpoints.\",\n    examples: [{ page: 0, pageSize: 10 }],\n  }),\n);\n\nexport type QueryType = typeof Query.Type;\n\nexport const PaginationMeta = Schema.Struct({\n  page: Schema.Int,\n  pageSize: Schema.Int,\n  total: Schema.Int,\n  pageCount: Schema.Int,\n}).pipe(\n  Schema.annotate({\n    identifier: \"PaginationMeta\",\n    title: \"Pagination Metadata\",\n    description: \"Pagination metadata returned with a paginated list response.\",\n    examples: [{ page: 0, pageSize: 10, total: 125, pageCount: 13 }],\n  }),\n);\n\nexport type PaginationMetaType = typeof PaginationMeta.Type;\n\nexport const PaginatedResponse = <A>(items: Schema.Schema<A>) =>\n  Schema.Struct({\n    data: Schema.Array(items),\n    meta: PaginationMeta,\n  });\n",
      "type": "registry:file",
      "target": "lib/query.ts"
    }
  ],
  "docs": "## Overview\n\n`pagination` provides reusable Effect schemas for API endpoints that accept paginated requests and return paginated lists. The request query uses zero-based `page` and bounded `pageSize` fields. The response helper wraps item arrays in `{ data, meta }`, where `meta` includes `page`, `pageSize`, `total`, and `pageCount`.\n\n## Included Files\n\n- `lib/query.ts` - `Query`, `PaginationMeta`, `PaginatedResponse`, and exported schema types\n\n## Usage\n\n```ts\nimport { Query, PaginatedResponse } from \"@/lib/query\";\nimport { Record } from \"@/services/record/schema\";\n\nexport const PaginatedRecords = PaginatedResponse(Record);\n\nHttpApiEndpoint.get(\"listRecords\", \"/records\", {\n  query: Query,\n  success: PaginatedRecords,\n});\n```\n\nReturn data with the matching envelope:\n\n```ts\nreturn {\n  data: rows,\n  meta: {\n    page,\n    pageSize,\n    total,\n    pageCount: Math.ceil(total / pageSize),\n  },\n};\n```",
  "type": "registry:lib"
}
